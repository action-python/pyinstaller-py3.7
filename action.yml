name: "PyInstaller Linux WIN64"
description: "GitHub Action package, Your python code to Binary for Linux WIN64 Architecture"
branding:
  icon: "box"
  color: "blue"
inputs:
  path:
    description: "Directory containing source code & .spec file (optional requirements.txt)."
    required: True
    default: src
  pypi_url:
    description: "Specify a custom URL for PYPI"
    required: False
    default: https://pypi.python.org/
  pypi_index_url:
    description: "Specify a custom URL for PYPI Index"
    required: False
    default: https://pypi.python.org/simple
  spec:
    description: "Specify a file path for .spec file"
    required: False
    default: ""
  requirements:
    description: 'Specify a file path for requirements.txt file'
    required: False
    default: "requirements.txt"
  rename:
    description: 'Specify is pyinstaller will create single executable file?'
    required: False
    default: ""
outputs:
  location:
    description: 'Binary file location or Directory location.'
  filename:
    description: 'Binary file name or NULL.'
  content_type:
    description: 'File content type or NULL.'

runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.path }}
    - ${{ inputs.pypi_url }}
    - ${{ inputs.pypi_index_url }}
    - ${{ inputs.spec }}
    - ${{ inputs.requirements }}
    - ${{ inputs.rename }}
